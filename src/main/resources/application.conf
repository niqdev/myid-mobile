url {
  base-path = "https://my.idmobile.ie"
  login = ${url.base-path}"/login"
  logout = ${url.base-path}"/logout"
  refresh = ${url.base-path}"/refresh-user"
  balance = ${url.base-path}
  history = ${url.base-path}"/my-activity"
}